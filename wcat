//Sojel Sunny
//Operating Systems Lab 0 A

#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int main(int argc, char *argv[] {
/*
//print out the number of arguments passed to the program
	printf("Num of args: %d\n", argc);

//print out the string for each argument
	for(int i = 0; i < argc; i++) {
		printf("%s\n", argv[i])
	}
	return 0;

*/ 

//useful functions
fopen(char *pathname, char *mode)

fclose(FILE *stream)


fgets(char *buffer, int buffer_size, FILE *stream)


getline(char **lineptr, size_t *len, FILE *stream)


strstr(char *haystack, char *needle)


}




/*
useful slide
fopen():

FILE *fp = fopen("main.c", "r");

if (fp == NULL) {

    printf("cannot open file\n");

    exit(1);

}
*/
